GIT COMMANDS~

git status
git add <filename>
git commit -m "updated content"
git push origin main

------------------------------------------------------------------------------------>

If you run into issues:

"Not a git repository" error → Run this from the root LEARNING folder instead:

bash  cd C:\Users\Lenovo\OneDrive\Desktop\LEARNING
  git add NodeJS/14_BloggingApp/
  git commit -m "Update BloggingApp files"
  git push origin main
  
-------------------------------------------------------------------------------------->
